
The GUI provides a user-friendly interface to:
1. Enter connection details (host, port, username)
2. Choose authentication method (password or SSH key)
3. Specify the remote path to check
4. View results in a formatted table

## Requirements

- Python 3.6+
- paramiko (for SSH connections)
- tkinter (usually included with Python)
